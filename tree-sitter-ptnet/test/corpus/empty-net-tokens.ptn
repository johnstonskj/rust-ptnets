=====================
Empty net with tokens
=====================

net example {
  tokens {Red, Green, Blue} * Natural
}

---

(source_file
  (net_def
    (name)
    (tokens
      (token_value
        (set_type
          (name)
          (name)
          (name)))
      (token_counter
        (token_set_natural)))))

