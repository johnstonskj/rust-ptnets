# Project ptnets

## Crates

- `ptnet-core` -- contains the traits and common *small* types used across all implementations.
* `ptnet-elementary` -- contains the implementation of the core traits for elementary nets.
